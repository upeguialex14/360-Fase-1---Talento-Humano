{
    "name": "talentohumano360-root",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix front",
        "build": "npm run build --prefix front",
        "lint": "npm run lint --prefix front",
        "preview": "npm run preview --prefix front"
    },
    "dependencies": {
        "multer": "^2.1.1",
        "xlsx": "^0.18.5"
    }
}
